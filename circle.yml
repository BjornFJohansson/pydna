machine:
  environment:
    PATH: /home/ubuntu/miniconda3/bin:$PATH
dependencies:
    override:
        - echo "deps"
        - bash --version
test:
    override:
        - bash ci.sh
deployment:
  release:
    tag: /.*/
    owner: circleci
    commands:
      - echo "deploy"
